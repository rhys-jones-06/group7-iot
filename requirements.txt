# LockIn — Internet of Things Project
# CM2211 Group 07
#
# Split into two files to handle platform-specific dependencies:
#
# Install on development machine (macOS, Windows, Linux):
#   pip install -r requirements-server.txt
#
# Install on Raspberry Pi:
#   pip install -r requirements-pi.txt
#
# Optional development:
#   pip install pytest python-dotenv
