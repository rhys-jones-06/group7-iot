# LockIn Raspberry Pi Requirements
# Install via setup.sh — do not pip install directly on the Pi.
# picamera2 and opencv are installed via apt (see setup.sh).

# Hardware I/O
RPi.GPIO==0.7.0

# Networking
requests==2.31.0
